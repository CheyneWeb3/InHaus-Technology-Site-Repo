import { FormEvent,useEffect,useState } from 'react';
import { Link } from 'react-router-dom';
import { api,jsonBody } from '../lib/api';
import type { Project } from '../lib/types';
import { Badge,Card,Empty,Field,Modal,PageTitle } from '../components/Ui';
export function Projects({kindFilter,title='Projects',subtitle='Canonical private records. Public projects are only a deliberately smaller projection.'}:{kindFilter?:string;title?:string;subtitle?:string}){const [items,setItems]=useState<Project[]>([]);const [open,setOpen]=useState(false);const [error,setError]=useState('');const load=()=>api<Project[]>('/projects').then(rows=>setItems(kindFilter?rows.filter(x=>x.kind===kindFilter):rows));useEffect(()=>{void load()},[kindFilter]);const create=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setError('');const fd=new FormData(e.currentTarget);try{await api('/projects',{method:'POST',body:jsonBody({name:fd.get('name'),slug:fd.get('slug'),kind:kindFilter||fd.get('kind'),summary:fd.get('summary')})});setOpen(false);await load();}catch(e:any){setError(e.message)}};return <><PageTitle title={title} subtitle={subtitle} actions={<button className="button primary" onClick={()=>setOpen(true)}>New {kindFilter==='game'?'game':'project'}</button>}/><Card>{items.length?<div className="project-list">{items.map(p=><Link to={`/admin/projects/${p.id}`} className="project-row" key={p.id}><div><div className="row-title">{p.name}</div><div className="row-sub">{p.summary||p.purpose||p.slug}</div></div><div className="row-meta"><Badge tone={p.status==='active'?'active':'neutral'}>{p.status}</Badge><Badge tone={p.published?'public':'private'}>{p.published?'public':'private'}</Badge><span>{p.kind}</span><time>{new Date(p.updated_at).toLocaleDateString()}</time></div></Link>)}</div>:<Empty>No {kindFilter==='game'?'game':'project'} records yet.</Empty>}</Card>{open&&<Modal title={`Create ${kindFilter==='game'?'game':'project'}`} onClose={()=>setOpen(false)}><form className="form" onSubmit={create}><Field label="Name"><input name="name" required/></Field><Field label="Slug"><input name="slug" required placeholder="haus-chain"/></Field>{!kindFilter&&<Field label="Kind"><select name="kind"><option>project</option><option>game</option><option>experiment</option><option>infrastructure</option><option>other</option></select></Field>}<Field label="Summary"><textarea name="summary" rows={4}/></Field>{error&&<p className="form-error">{error}</p>}<button className="button primary">Create project shell</button></form></Modal>}</>}
