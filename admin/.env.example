# Browser-side admin settings. Configure these in Netlify for the production admin build.
VITE_API_BASE=http://127.0.0.1:8080/api
# Optional QR/mobile wallet support. Injected browser wallets work without this value.
VITE_WALLETCONNECT_PROJECT_ID=
