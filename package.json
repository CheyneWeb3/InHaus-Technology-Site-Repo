{
  "name": "inhaus-technology-business-site",
  "version": "1.7.18",
  "private": true,
  "type": "module",
  "description": "Static JSON-driven InHaus Technology business, project and gaming site.",
  "scripts": {
    "dev": "node scripts/serve.mjs --root . --port 49215",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs --root dist --port 49216",
    "start": "node scripts/serve.mjs --root dist --port 49217",
    "check": "npm run build && npm run verify",
    "verify": "node scripts/verify.mjs",
    "build:admin": "npm ci --prefix admin && npm run build --prefix admin",
    "build:netlify": "npm run check && npm run build:admin"
  },
  "engines": {
    "node": ">=18.18.0"
  },
  "packageManager": "npm@10.8.2"
}
