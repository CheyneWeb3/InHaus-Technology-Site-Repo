{
  "name": "inhaus-technologies-business-site",
  "version": "1.7.2",
  "private": true,
  "type": "module",
  "description": "Static JSON-driven InHaus Technologies business, project and gaming site.",
  "scripts": {
    "dev": "node scripts/serve.mjs --root . --port 49215",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs --root dist --port 49216",
    "start": "node scripts/serve.mjs --root dist --port 49217",
    "check": "npm run build && npm run verify",
    "verify": "node scripts/verify.mjs"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
